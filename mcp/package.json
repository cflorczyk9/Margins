{
  "name": "@margins/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Margins MCP connector — exposes a Margins vault to Claude / ChatGPT over stdio.",
  "type": "module",
  "bin": {
    "margins-mcp": "./bin/margins-mcp.js"
  },
  "scripts": {
    "start": "node bin/margins-mcp.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  }
}
